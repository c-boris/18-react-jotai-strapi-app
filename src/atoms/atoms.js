import { atom } from 'jotai';

// Définissez l'atome userAtom
export const userAtom = atom(null);

// Définissez d'autres atomes si nécessaire
export const tokenAtom = atom(null);
export const uidAtom = atom(null);
